import { UIEventHandler } from "react";
declare const useLayoutScrolledContent: () => {
    isContentScrolled: boolean;
    onScroll: UIEventHandler<HTMLDivElement>;
};
export default useLayoutScrolledContent;
